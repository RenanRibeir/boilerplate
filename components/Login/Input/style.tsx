import { StyleSheet } from 'react-native';

export default StyleSheet.create({

  input: {    
    width: '80%',
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 10,
    borderWidth: 1,
  },

});