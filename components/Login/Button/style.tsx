import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  button: {
    color: '#fff',
    backgroundColor: '#000',
    width: '80%',
    height: '7%',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
