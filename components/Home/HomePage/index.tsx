import React from 'react';
import {
  SafeAreaView,
  Text,
} from 'react-native';

import NavBar from '../Navbar';

const HomePage = () => {

  return (
    <NavBar/>
  );
};

export default HomePage;